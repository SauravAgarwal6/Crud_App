# CRUD App

This project is a **simple CRUD (Create, Read, Update, Delete) web application** built using:

- **HTML**
- **CSS**
- **JavaScript**
- **Backend using JSON (db.json)**

It allows users to manage employee data through a basic frontend interface.

## 📁 Folder Structure

